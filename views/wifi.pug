extends layout

block content
  h1= title
  p WiFi Status - #{connection.msg}
     
      h3 SSID: #{connection.ssid}
      h3 Mode: #{connection.mode}
      h3 Level: #{connection.signal_level}
  p
   ul
     li 
       a(href="/wan/wifi/networks") Select network
     li 
       a(href="/wan/wifi/reset") Reset WiFi

  br
  a(href="/wan") Back
                                           

